#fuction to find the max min

def find_max_min(numbers):
integers is list [1,2,3,4]
if (len(int) .1:
    if max(numbers) == min(integer)):
               return [len(integer)]
    elase
                return[min(integers), max (numbers
#this fuction has errors tried debuging to no avail
